// app.js — version incorrecte pour démonstration
const PI = 3.14159;

function calculerAire(rayon) {
    return PI * rayon ** 2;
}

console.log(calculerAire(2));

// const PI = 3.14159; // ❌ Erreur : Identifier 'PI' has already been declared
